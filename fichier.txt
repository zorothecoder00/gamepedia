Dans Next.js App Router, tous les composants sont
  Server Components par défaut. Les event handlers (onMouseEnter, onClick, etc.)
   et les hooks React (useState, useEffect) ne fonctionnent que côté client — il
   faut donc déclarer "use client" en première ligne pour les fichiers qui les
  utilisent.



-Vérifie si tu as déjà codé la page utilisateur ou c'est ça tu as appelé /profile ( différente de l'admin qui a une vue globale et gère tout)

-Reste hors admin, si tu veux continuer un jour : /profile/edit,
  /rankings/[gameSlug]/[seasonId], /tournaments/[slug]/bracket (plein écran), et
  les pages d'erreur /error + /not-found. Rien d'urgent — dis-moi si tu veux
  que j'en prenne une.


 Toutes les pages de docs/pages.md sont désormais présentes. Correction de mon
  audit du départ : la doc annonçait ~34 pages ; il en manquait réellement moins
  que je ne le pensais (les pages d'erreur existaient). Il reste surtout, si tu
  veux pousser la qualité :
  - brancher le vrai upload d'images (les routes /api/upload/* sont des stubs
  qui renvoient une URL factice) ;
  - un vrai rendu Markdown pour les articles (actuellement un parseur maison
  minimal) ;
  - l'auth (les tokens sont des demo-token-<id>, à remplacer par du JWT).

